logging:
  level:
    fr:
      alexandreroman: DEBUG
    org:
      hibernate: INFO

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
    show-sql: false
  cloud:
    discovery:
      enabled: false

ribbon:
  eureka:
    enabled: false
